import { FocusGroupDirective } from './focus-group.directive';

describe('FocusGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusGroupDirective();
    expect(directive).toBeTruthy();
  });
});
