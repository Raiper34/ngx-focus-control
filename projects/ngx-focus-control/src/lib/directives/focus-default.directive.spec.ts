import { FocusDefaultDirective } from './focus-default.directive';

describe('FocusDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusDefaultDirective(null);
    expect(directive).toBeTruthy();
  });
});
